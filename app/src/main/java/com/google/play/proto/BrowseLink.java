// Code generated by Wire protocol buffer compiler, do not edit.
// Source file: proto/Browse.proto
package com.google.play.proto;

import com.squareup.wire.Message;
import com.squareup.wire.ProtoField;
import okio.ByteString;

import static com.squareup.wire.Message.Datatype.BYTES;
import static com.squareup.wire.Message.Datatype.STRING;

public final class BrowseLink extends Message {

  public static final String DEFAULT_NAME = "";
  public static final String DEFAULT_DATAURL = "";
  public static final ByteString DEFAULT_SERVERLOGSCOOKIE = ByteString.EMPTY;

  @ProtoField(tag = 1, type = STRING)
  public final String name;

  @ProtoField(tag = 3, type = STRING)
  public final String dataUrl;

  @ProtoField(tag = 4, type = BYTES)
  public final ByteString serverLogsCookie;

  public BrowseLink(String name, String dataUrl, ByteString serverLogsCookie) {
    this.name = name;
    this.dataUrl = dataUrl;
    this.serverLogsCookie = serverLogsCookie;
  }

  private BrowseLink(Builder builder) {
    this(builder.name, builder.dataUrl, builder.serverLogsCookie);
    setBuilder(builder);
  }

  @Override
  public boolean equals(Object other) {
    if (other == this) return true;
    if (!(other instanceof BrowseLink)) return false;
    BrowseLink o = (BrowseLink) other;
    return equals(name, o.name)
        && equals(dataUrl, o.dataUrl)
        && equals(serverLogsCookie, o.serverLogsCookie);
  }

  @Override
  public int hashCode() {
    int result = hashCode;
    if (result == 0) {
      result = name != null ? name.hashCode() : 0;
      result = result * 37 + (dataUrl != null ? dataUrl.hashCode() : 0);
      result = result * 37 + (serverLogsCookie != null ? serverLogsCookie.hashCode() : 0);
      hashCode = result;
    }
    return result;
  }

  public static final class Builder extends Message.Builder<BrowseLink> {

    public String name;
    public String dataUrl;
    public ByteString serverLogsCookie;

    public Builder() {
    }

    public Builder(BrowseLink message) {
      super(message);
      if (message == null) return;
      this.name = message.name;
      this.dataUrl = message.dataUrl;
      this.serverLogsCookie = message.serverLogsCookie;
    }

    public Builder name(String name) {
      this.name = name;
      return this;
    }

    public Builder dataUrl(String dataUrl) {
      this.dataUrl = dataUrl;
      return this;
    }

    public Builder serverLogsCookie(ByteString serverLogsCookie) {
      this.serverLogsCookie = serverLogsCookie;
      return this;
    }

    @Override
    public BrowseLink build() {
      return new BrowseLink(this);
    }
  }
}
