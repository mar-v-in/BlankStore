// Code generated by Wire protocol buffer compiler, do not edit.
// Source file: proto/Music.proto
package com.google.play.proto;

import com.squareup.wire.Message;
import com.squareup.wire.ProtoField;

import static com.squareup.wire.Message.Datatype.STRING;

public final class AlbumDetails extends Message {

  public static final String DEFAULT_NAME = "";

  @ProtoField(tag = 1, type = STRING)
  public final String name;

  @ProtoField(tag = 2)
  public final MusicDetails details;

  @ProtoField(tag = 3)
  public final ArtistDetails displayArtist;

  public AlbumDetails(String name, MusicDetails details, ArtistDetails displayArtist) {
    this.name = name;
    this.details = details;
    this.displayArtist = displayArtist;
  }

  private AlbumDetails(Builder builder) {
    this(builder.name, builder.details, builder.displayArtist);
    setBuilder(builder);
  }

  @Override
  public boolean equals(Object other) {
    if (other == this) return true;
    if (!(other instanceof AlbumDetails)) return false;
    AlbumDetails o = (AlbumDetails) other;
    return equals(name, o.name)
        && equals(details, o.details)
        && equals(displayArtist, o.displayArtist);
  }

  @Override
  public int hashCode() {
    int result = hashCode;
    if (result == 0) {
      result = name != null ? name.hashCode() : 0;
      result = result * 37 + (details != null ? details.hashCode() : 0);
      result = result * 37 + (displayArtist != null ? displayArtist.hashCode() : 0);
      hashCode = result;
    }
    return result;
  }

  public static final class Builder extends Message.Builder<AlbumDetails> {

    public String name;
    public MusicDetails details;
    public ArtistDetails displayArtist;

    public Builder() {
    }

    public Builder(AlbumDetails message) {
      super(message);
      if (message == null) return;
      this.name = message.name;
      this.details = message.details;
      this.displayArtist = message.displayArtist;
    }

    public Builder name(String name) {
      this.name = name;
      return this;
    }

    public Builder details(MusicDetails details) {
      this.details = details;
      return this;
    }

    public Builder displayArtist(ArtistDetails displayArtist) {
      this.displayArtist = displayArtist;
      return this;
    }

    @Override
    public AlbumDetails build() {
      return new AlbumDetails(this);
    }
  }
}
