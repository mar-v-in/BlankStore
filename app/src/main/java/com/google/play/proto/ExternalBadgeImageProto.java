// Code generated by Wire protocol buffer compiler, do not edit.
// Source file: proto/BadgeImage.proto
package com.google.play.proto;

import com.squareup.wire.Message;
import com.squareup.wire.ProtoField;

import static com.squareup.wire.Message.Datatype.INT32;
import static com.squareup.wire.Message.Datatype.STRING;

public final class ExternalBadgeImageProto extends Message {

  public static final Integer DEFAULT_USAGE = 0;
  public static final String DEFAULT_URL = "";

  @ProtoField(tag = 1, type = INT32)
  public final Integer usage;

  @ProtoField(tag = 2, type = STRING)
  public final String url;

  public ExternalBadgeImageProto(Integer usage, String url) {
    this.usage = usage;
    this.url = url;
  }

  private ExternalBadgeImageProto(Builder builder) {
    this(builder.usage, builder.url);
    setBuilder(builder);
  }

  @Override
  public boolean equals(Object other) {
    if (other == this) return true;
    if (!(other instanceof ExternalBadgeImageProto)) return false;
    ExternalBadgeImageProto o = (ExternalBadgeImageProto) other;
    return equals(usage, o.usage)
        && equals(url, o.url);
  }

  @Override
  public int hashCode() {
    int result = hashCode;
    if (result == 0) {
      result = usage != null ? usage.hashCode() : 0;
      result = result * 37 + (url != null ? url.hashCode() : 0);
      hashCode = result;
    }
    return result;
  }

  public static final class Builder extends Message.Builder<ExternalBadgeImageProto> {

    public Integer usage;
    public String url;

    public Builder() {
    }

    public Builder(ExternalBadgeImageProto message) {
      super(message);
      if (message == null) return;
      this.usage = message.usage;
      this.url = message.url;
    }

    public Builder usage(Integer usage) {
      this.usage = usage;
      return this;
    }

    public Builder url(String url) {
      this.url = url;
      return this;
    }

    @Override
    public ExternalBadgeImageProto build() {
      return new ExternalBadgeImageProto(this);
    }
  }
}
