// Code generated by Wire protocol buffer compiler, do not edit.
// Source file: proto/Download.proto
package com.google.play.proto;

import com.squareup.wire.Message;
import com.squareup.wire.ProtoField;

import static com.squareup.wire.Message.Datatype.INT32;
import static com.squareup.wire.Message.Datatype.STRING;

public final class EncryptionParams extends Message {

  public static final Integer DEFAULT_VERSION = 0;
  public static final String DEFAULT_ENCRYPTIONKEY = "";
  public static final String DEFAULT_HMACKEY = "";

  @ProtoField(tag = 1, type = INT32)
  public final Integer version;

  @ProtoField(tag = 2, type = STRING)
  public final String encryptionKey;

  @ProtoField(tag = 3, type = STRING)
  public final String hmacKey;

  public EncryptionParams(Integer version, String encryptionKey, String hmacKey) {
    this.version = version;
    this.encryptionKey = encryptionKey;
    this.hmacKey = hmacKey;
  }

  private EncryptionParams(Builder builder) {
    this(builder.version, builder.encryptionKey, builder.hmacKey);
    setBuilder(builder);
  }

  @Override
  public boolean equals(Object other) {
    if (other == this) return true;
    if (!(other instanceof EncryptionParams)) return false;
    EncryptionParams o = (EncryptionParams) other;
    return equals(version, o.version)
        && equals(encryptionKey, o.encryptionKey)
        && equals(hmacKey, o.hmacKey);
  }

  @Override
  public int hashCode() {
    int result = hashCode;
    if (result == 0) {
      result = version != null ? version.hashCode() : 0;
      result = result * 37 + (encryptionKey != null ? encryptionKey.hashCode() : 0);
      result = result * 37 + (hmacKey != null ? hmacKey.hashCode() : 0);
      hashCode = result;
    }
    return result;
  }

  public static final class Builder extends Message.Builder<EncryptionParams> {

    public Integer version;
    public String encryptionKey;
    public String hmacKey;

    public Builder() {
    }

    public Builder(EncryptionParams message) {
      super(message);
      if (message == null) return;
      this.version = message.version;
      this.encryptionKey = message.encryptionKey;
      this.hmacKey = message.hmacKey;
    }

    public Builder version(Integer version) {
      this.version = version;
      return this;
    }

    public Builder encryptionKey(String encryptionKey) {
      this.encryptionKey = encryptionKey;
      return this;
    }

    public Builder hmacKey(String hmacKey) {
      this.hmacKey = hmacKey;
      return this;
    }

    @Override
    public EncryptionParams build() {
      return new EncryptionParams(this);
    }
  }
}
