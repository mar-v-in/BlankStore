// Code generated by Wire protocol buffer compiler, do not edit.
// Source file: proto/Carrier.proto
package com.google.play.proto;

import com.squareup.wire.Message;
import com.squareup.wire.ProtoField;

import static com.squareup.wire.Message.Datatype.INT32;
import static com.squareup.wire.Message.Datatype.STRING;

public final class EncryptedSubscriberInfo extends Message {

  public static final String DEFAULT_DATA = "";
  public static final String DEFAULT_ENCRYPTEDKEY = "";
  public static final String DEFAULT_SIGNATURE = "";
  public static final String DEFAULT_INITVECTOR = "";
  public static final Integer DEFAULT_GOOGLEKEYVERSION = 0;
  public static final Integer DEFAULT_CARRIERKEYVERSION = 0;

  @ProtoField(tag = 1, type = STRING)
  public final String data;

  @ProtoField(tag = 2, type = STRING)
  public final String encryptedKey;

  @ProtoField(tag = 3, type = STRING)
  public final String signature;

  @ProtoField(tag = 4, type = STRING)
  public final String initVector;

  @ProtoField(tag = 5, type = INT32)
  public final Integer googleKeyVersion;

  @ProtoField(tag = 6, type = INT32)
  public final Integer carrierKeyVersion;

  public EncryptedSubscriberInfo(String data, String encryptedKey, String signature, String initVector, Integer googleKeyVersion, Integer carrierKeyVersion) {
    this.data = data;
    this.encryptedKey = encryptedKey;
    this.signature = signature;
    this.initVector = initVector;
    this.googleKeyVersion = googleKeyVersion;
    this.carrierKeyVersion = carrierKeyVersion;
  }

  private EncryptedSubscriberInfo(Builder builder) {
    this(builder.data, builder.encryptedKey, builder.signature, builder.initVector, builder.googleKeyVersion, builder.carrierKeyVersion);
    setBuilder(builder);
  }

  @Override
  public boolean equals(Object other) {
    if (other == this) return true;
    if (!(other instanceof EncryptedSubscriberInfo)) return false;
    EncryptedSubscriberInfo o = (EncryptedSubscriberInfo) other;
    return equals(data, o.data)
        && equals(encryptedKey, o.encryptedKey)
        && equals(signature, o.signature)
        && equals(initVector, o.initVector)
        && equals(googleKeyVersion, o.googleKeyVersion)
        && equals(carrierKeyVersion, o.carrierKeyVersion);
  }

  @Override
  public int hashCode() {
    int result = hashCode;
    if (result == 0) {
      result = data != null ? data.hashCode() : 0;
      result = result * 37 + (encryptedKey != null ? encryptedKey.hashCode() : 0);
      result = result * 37 + (signature != null ? signature.hashCode() : 0);
      result = result * 37 + (initVector != null ? initVector.hashCode() : 0);
      result = result * 37 + (googleKeyVersion != null ? googleKeyVersion.hashCode() : 0);
      result = result * 37 + (carrierKeyVersion != null ? carrierKeyVersion.hashCode() : 0);
      hashCode = result;
    }
    return result;
  }

  public static final class Builder extends Message.Builder<EncryptedSubscriberInfo> {

    public String data;
    public String encryptedKey;
    public String signature;
    public String initVector;
    public Integer googleKeyVersion;
    public Integer carrierKeyVersion;

    public Builder() {
    }

    public Builder(EncryptedSubscriberInfo message) {
      super(message);
      if (message == null) return;
      this.data = message.data;
      this.encryptedKey = message.encryptedKey;
      this.signature = message.signature;
      this.initVector = message.initVector;
      this.googleKeyVersion = message.googleKeyVersion;
      this.carrierKeyVersion = message.carrierKeyVersion;
    }

    public Builder data(String data) {
      this.data = data;
      return this;
    }

    public Builder encryptedKey(String encryptedKey) {
      this.encryptedKey = encryptedKey;
      return this;
    }

    public Builder signature(String signature) {
      this.signature = signature;
      return this;
    }

    public Builder initVector(String initVector) {
      this.initVector = initVector;
      return this;
    }

    public Builder googleKeyVersion(Integer googleKeyVersion) {
      this.googleKeyVersion = googleKeyVersion;
      return this;
    }

    public Builder carrierKeyVersion(Integer carrierKeyVersion) {
      this.carrierKeyVersion = carrierKeyVersion;
      return this;
    }

    @Override
    public EncryptedSubscriberInfo build() {
      return new EncryptedSubscriberInfo(this);
    }
  }
}
