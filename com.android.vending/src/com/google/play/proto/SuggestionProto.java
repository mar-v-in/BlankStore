// Code generated by Wire protocol buffer compiler, do not edit.
// Source file: proto/Query.proto
package com.google.play.proto;

import com.squareup.wire.Message;
import com.squareup.wire.ProtoField;

public final class SuggestionProto extends Message {

  @ProtoField(tag = 2)
  public final AppSuggestionProto appSuggestion;

  @ProtoField(tag = 3)
  public final QuerySuggestionProto querySuggestion;

  public SuggestionProto(AppSuggestionProto appSuggestion, QuerySuggestionProto querySuggestion) {
    this.appSuggestion = appSuggestion;
    this.querySuggestion = querySuggestion;
  }

  private SuggestionProto(Builder builder) {
    this(builder.appSuggestion, builder.querySuggestion);
    setBuilder(builder);
  }

  @Override
  public boolean equals(Object other) {
    if (other == this) return true;
    if (!(other instanceof SuggestionProto)) return false;
    SuggestionProto o = (SuggestionProto) other;
    return equals(appSuggestion, o.appSuggestion)
        && equals(querySuggestion, o.querySuggestion);
  }

  @Override
  public int hashCode() {
    int result = hashCode;
    if (result == 0) {
      result = appSuggestion != null ? appSuggestion.hashCode() : 0;
      result = result * 37 + (querySuggestion != null ? querySuggestion.hashCode() : 0);
      hashCode = result;
    }
    return result;
  }

  public static final class Builder extends Message.Builder<SuggestionProto> {

    public AppSuggestionProto appSuggestion;
    public QuerySuggestionProto querySuggestion;

    public Builder() {
    }

    public Builder(SuggestionProto message) {
      super(message);
      if (message == null) return;
      this.appSuggestion = message.appSuggestion;
      this.querySuggestion = message.querySuggestion;
    }

    public Builder appSuggestion(AppSuggestionProto appSuggestion) {
      this.appSuggestion = appSuggestion;
      return this;
    }

    public Builder querySuggestion(QuerySuggestionProto querySuggestion) {
      this.querySuggestion = querySuggestion;
      return this;
    }

    @Override
    public SuggestionProto build() {
      return new SuggestionProto(this);
    }
  }
}
